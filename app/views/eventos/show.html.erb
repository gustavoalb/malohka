<%#- model_class = Evento -%>
<%- model_class = Evento -%>
<div class="page-header" align='center'>
  <% if @evento.banner_file_name.present? %>
    <%= image_tag(@evento.banner(:banner)) %>
  <% else %>
    <h1 align='center'><%= @evento.nome %></h1>
  <% end %>
</div>
<%= render :partial => 'showw' %>
<br>
<!-- <div align='center'>
  <h4><a data-toggle="collapse" data-parent="#accordion" href="#coordenacao">Coordenação</a></h4>
  <div class="panel panel-default">
    <div id="coordenacao" class="panel-collapse collapse">
      <dl class="dl-horizontal">
        <dt><strong>Coordenação:</strong></dt>
        <dd><%#= @evento.responsavel.nome %></dd>
        <dt><strong>Status:</strong></dt>
        <dd><%#= @evento.status %></dd>
      </dl>
    </div>
  </div>

</div> -->
</div>
</div>
<%= link_to t('.back', :default => t("helpers.links.voltar")),
              eventos_path, :class => 'btn btn-default'  %>
<% if can? :edit, Evento %>
  <%= link_to t('.edit', :default => t("helpers.links.editar")),
              edit_evento_path(@evento), :class => 'btn btn-default' %>
<% end %>
<% if can? :destroy, Evento %>
  <%= link_to t('.destroy', :default => t("helpers.links.apagar")),
              evento_path(@evento),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
<% end %>
</div>