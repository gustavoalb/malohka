<%- model_class = Nivel -%>
<div class="page-header">
  <h1 align='center'>Nível <%= @nivel.nome %></h1>
</div>
<div class="col-sm-12 col-md-12, row, well">
  <dl class="dl-horizontal">
    <dt><strong><%= model_class.human_attribute_name(:id) %>:</strong></dt>
    <dd><%= @nivel.id %></dd>
    <dt><strong><%= model_class.human_attribute_name(:codigo) %>:</strong></dt>
    <dd><%= @nivel.codigo %></dd>
    <dt><strong>Cadastrado em:</strong></dt>
    <dd><%= @nivel.created_at.strftime("%d/%m/%Y %H:%m") %></dd>
  </dl>
</div>
<%= link_to t('.back', :default => t("helpers.links.voltar")),
              niveis_path, :class => 'btn btn-default'  %>
<%= link_to t('.edit', :default => t("helpers.links.editar")),
              edit_nivel_path(@nivel), :class => 'btn btn-default' %>
<%= link_to t('.destroy', :default => t("helpers.links.apagar")),
              nivel_path(@nivel),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>