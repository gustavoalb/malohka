<%= simple_form_for @nivel, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="col-sm-12 col-md-12, row, well">
    <%= f.input :nome, label: 'Nome' %>
    <%= f.input :codigo, label: 'Código' %>
    <%= f.submit "Confirmar",:class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancelar")),
                niveis_path, :class => 'btn btn-default' %>
  </div>
<% end %>